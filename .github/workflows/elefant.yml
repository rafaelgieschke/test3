on: push
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - run: |
        df -h
        ls -laR /mnt
        fdisk -l /dev/sda
        fdisk -l /dev/sdb
        ls -la /dev
        mount
        blkid
